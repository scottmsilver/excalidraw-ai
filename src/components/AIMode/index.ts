/**
 * AI Mode Components
 *
 * Components and hooks for managing AI mode in the canvas.
 * When active, users can Shift+Click to place markers while drawing normally.
 */

export { useAIMode, default as useAIModeDefault } from "./useAIMode";
export type { AIToolState, AIToolActions, UseAIModeResult } from "./types";
